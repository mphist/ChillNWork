import React from "react";
import Media from "../components/Media";

const MediaContainer = () => {
  return (
    <div className="media-container" style={{ textAlign: "center" }}>
      <Media />
    </div>
  );
};

export default MediaContainer;
